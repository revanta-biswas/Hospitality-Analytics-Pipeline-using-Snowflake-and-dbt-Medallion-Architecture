# SonarQube configuration — generated by AIRE. Only ${SLOT} values vary (Section 4.1.1).
# 🔴 No token belongs in this file. SONAR_TOKEN / SONAR_HOST_URL are GitHub Actions secrets only.

sonar.projectKey=revanta-biswas_Hospitality-Analytics-Pipeline-using-Snowflake-and-dbt-Medallion-Architecture
sonar.projectName=Hospitality-Analytics-Pipeline-using-Snowflake-and-dbt-Medallion-Architecture
sonar.organization=revanta-biswas

# 🔴 sonar.sources / sonar.tests below are REWRITTEN AT RUN TIME by the pipeline's
#    "Resolve Sonar scope" step, down to the paths that actually exist in the checkout.
#    A path here that does not exist yet (greenfield src/) would otherwise fail the scan
#    outright with "The folder 'src' does not exist" and take the quality gate with it.
sonar.sources=src/dbt_code,src/snowflake
sonar.exclusions=**/target/**,**/dbt_packages/**,**/logs/**,**/.venv/**,**/venv/**

# Coverage report path for the detected test runner: this repo (tests/.evals/config.json ci.roots[0])
# has coverageCommand=null / coverageReportPath=null — a dbt/SQL project with no traditional Python
# application code and no pytest suite; story ACs are verified via `dbt parse`/`dbt compile`
# (REQ-NF-02), not unit-test coverage. No sonar.python.coverage.reportPaths line is emitted because
# there is no real report to point it at yet — inventing one here would be exactly the "written from
# intent, not from what exists" defect Section 4.1.1's own Resolve Sonar scope step exists to catch.
# Add sonar.python.coverage.reportPaths=<path> the moment a coverage.xml is genuinely produced.

sonar.sourceEncoding=UTF-8
